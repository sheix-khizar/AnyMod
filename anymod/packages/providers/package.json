{
  "name": "@anymod/providers",
  "version": "0.1.0",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "dependencies": {
    "@anymod/types": "workspace:*",
    "groq-sdk": "*",
    "tiktoken": "*"
  },
  "devDependencies": {
    "@types/node": "*",
    "vitest": "*"
  },
  "scripts": {
    "build": "tsc -b",
    "test": "vitest run",
    "lint": "eslint src"
  }
}
