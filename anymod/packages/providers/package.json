{
  "name": "@anymod/providers",
  "version": "0.1.0",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "dependencies": {
    "@anymod/types": "workspace:*",
    "groq-sdk": "*",
    "tiktoken": "*"
  },
  "devDependencies": {
    "@types/node": "*",
    "dotenv": "^17.4.2",
    "vitest": "4.1.10"
  },
  "scripts": {
    "build": "tsc -b",
    "test": "vitest run --passWithNoTests",
    "lint": "eslint src"
  }
}
