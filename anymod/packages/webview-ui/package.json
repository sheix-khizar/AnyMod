{
  "name": "@anymod/webview-ui",
  "version": "0.1.0",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "dependencies": {
    "@anymod/types": "workspace:*"
  },
  "scripts": {
    "build": "vite build",
    "dev": "vite dev",
    "test": "vitest run --passWithNoTests",
    "lint": "eslint src"
  }
}
